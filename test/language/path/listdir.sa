import path

print("Testing path.listdir('.')...")

files = path.listdir(".")

assert("Makefile" in files, "✗ Makefile not found")
assert("src" in files, "✗ src directory not found")

print("path Tests Passed!")